diff --git a/src/plugins/certgen/script/certgen.sh.txt b/src/plugins/certgen/script/certgen.sh.txt
--- a/src/plugins/certgen/script/certgen.sh.txt
+++ b/src/plugins/certgen/script/certgen.sh.txt
@@ -1,2 +1,2 @@
 #!/bin/sh
-printf '%s' "$1" | openssl req -x509 -key /dev/stdin -subj "/C=XX/ST=StateName/L=CityName/O=CompanyName/OU=CompanySectionName/CN=my_slurm_ca"
+printf '%s' "$1" | openssl req -new -x509 -key /dev/stdin -subj "/C=XX/ST=StateName/L=CityName/O=CompanyName/OU=CompanySectionName/CN=my_slurm_ca"
diff --git a/src/plugins/certgen/script/certgen_script.c b/src/plugins/certgen/script/certgen_script.c
--- a/src/plugins/certgen/script/certgen_script.c
+++ b/src/plugins/certgen/script/certgen_script.c
@@ -66,10 +66,10 @@ extern void fini(void)
 	debug("unloaded");
 }
 
-static char *_exec_script(char *script_path, char *input)
+static char *_run_script(char *script_name, char *script_path,
+			 char **script_argv)
 {
 	int rc = SLURM_SUCCESS;
-	char **script_argv = NULL;
 	int status = SLURM_ERROR;
 	bool timed_out = false;
 	char *output = NULL;
@@ -80,10 +80,6 @@ static char *_exec_script(char *script_path, char *input)
 		.timed_out = &timed_out,
 	};
 
-	script_argv = xcalloc(3, sizeof(char *)); /* NULL terminated */
-	script_argv[0] = script_path;
-	script_argv[1] = input;
-
 	run_command_args.script_path = script_path;
 	run_command_args.script_argv = script_argv;
 
@@ -91,62 +87,48 @@ static char *_exec_script(char *script_path, char *input)
 
 	if (timed_out) {
 		error("%s: Timed out running script '%s'",
-		      plugin_type, script_path);
+		      plugin_type, script_name);
 		rc = SLURM_ERROR;
 	} else if (status) {
 		error("%s: '%s' returned rc %d. stdout+stderr from script:\n%s",
-		      plugin_type, script_path, status, output);
+		      plugin_type, script_name, status, output);
 		rc = SLURM_ERROR;
 	} else if (!output || !*output) {
 		error("%s: Expected output from '%s', but got nothing",
-		      plugin_type, script_path);
+		      plugin_type, script_name);
 		rc = SLURM_ERROR;
 	}
 
-	xfree(script_argv);
-
 	if (rc)
 		xfree(output);
 
 	return output;
 }
 
-static int _create_exec_script(char *name, char *contents, char **script_path)
+static char *_exec_script(char *script_path, char *input)
 {
-	int fd = -1;
+	char *script_argv[] = { script_path, input, NULL };
 
-	xassert(script_path);
+	return _run_script(script_path, script_path, script_argv);
+}
 
-	if ((fd = dump_to_memfd(name, contents, script_path)) < 0) {
-		error("%s: Failed to create script file", plugin_type);
-		xfree(*script_path);
-	}
+static char *_exec_internal_script(char *name, char *contents, char *input)
+{
+	char *script_argv[] = {
+		"/bin/sh", "-c", contents, name, input, NULL
+	};
 
-	return fd;
+	return _run_script(name, script_argv[0], script_argv);
 }
 
 static char *_exec_internal_keygen(void)
 {
 	char *keygen_contents = NULL;
 	char *name = "keygen.sh";
-	char *script_path = NULL;
-	int script_fd = -1;
 	char *key = NULL;
 
 	static_ref_to_cstring(keygen_contents, keygen_sh_txt);
-
-	script_fd = _create_exec_script(name, keygen_contents, &script_path);
-	if (script_fd < 0) {
-		error("%s: Failed to create executable script '%s'",
-		      plugin_type, name);
-		xfree(keygen_contents);
-		return NULL;
-	}
-
-	key = _exec_script(script_path, NULL);
-
-	close(script_fd);
-	xfree(script_path);
+	key = _exec_internal_script(name, keygen_contents, NULL);
 	xfree(keygen_contents);
 
 	return key;
@@ -156,24 +138,10 @@ static char *_exec_internal_certgen(char *key)
 {
 	char *certgen_contents = NULL;
 	char *name = "certgen.sh";
-	char *script_path = NULL;
-	int script_fd = -1;
 	char *cert = NULL;
 
 	static_ref_to_cstring(certgen_contents, certgen_sh_txt);
-
-	script_fd = _create_exec_script(name, certgen_contents, &script_path);
-	if (script_fd < 0) {
-		error("%s: Failed to create executable script '%s'",
-		      plugin_type, name);
-		xfree(certgen_contents);
-		return NULL;
-	}
-
-	cert = _exec_script(script_path, key);
-
-	close(script_fd);
-	xfree(script_path);
+	cert = _exec_internal_script(name, certgen_contents, key);
 	xfree(certgen_contents);
 
 	return cert;
