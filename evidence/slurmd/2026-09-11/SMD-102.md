# SMD-102 UID/GID不一致時のfail behavior

## 判定

- 状態: `BLOCKED_SECURITY_SKIPPED`
- 対象: PC-210 / macOS / arm64 worker上のproduction slurmd
- controller: ubuntu2504 / Ubuntu 24.04 / x86-64
- 目的: controllerとworkerで同名userのnumeric UID/GIDが異なるとき、誤ったidentityでjobを実行しないか確認する

## SMD-101からの分離

SMD-101は両nodeで一致する2 userを使い、Jobs 355/356で正しいidentity、所有権、相互隔離、accounting、
資源解放を確認して`PASS`した。SMD-102は正常系を繰り返さず、意図的なID不一致だけを変更変数とする。

## ソースから確認できたこと

現行sourceでは、job credential作成時にcontroller側のpasswd/group情報を`identity_t`へ格納し、UID、GID、
user名、HOME、shell、supplementary GIDをpackする。

- [`src/interfaces/cred.c`](../../../../src/interfaces/cred.c): `fetch_identity(arg->uid, arg->gid, ...)`
- [`src/common/identity.c`](../../../../src/common/identity.c): passwd/groupからidentityを構築してpack

workerのslurmstepdではcredentialのnumeric UID/GIDをそのままstep recordへ設定する。

- [`src/slurmd/slurmstepd/slurmstepd_job.c`](../../../../src/slurmd/slurmstepd/slurmstepd_job.c):
  `step->uid = cred_arg->uid`、`step->gid = cred_arg->gid`
- [`src/slurmd/common/privileges.c`](../../../../src/slurmd/common/privileges.c):
  `setegid(step->gid)`、`setgroups(...)`、`seteuid(step->uid)`

確認した経路には、workerのlocal passwdで`pw_name`を引き直してcredentialのUID/GIDと一致確認する処理は
見つからなかった。これは「不一致jobが必ず実行される」という実測結果ではなく、fail-openの可能性を示す
source上の仮説である。

## Research Questionと仮説

RQ: controllerではUID/GID 3201、workerでは同名だがUID/GID 3202の隔離userからjobを投入した場合、
macOS slurmdはjobを拒否するか、controller由来のraw numeric IDで実行するか。

- H1（望ましい結果）: local identity不一致を検知し、payload実行前に明確なerrorでfail closedする。
- H2（sourceから懸念する結果）: workerにUID 3201のpasswd entryがなくても`seteuid(3201)`が成立し、raw UID/GID
  3201でpayloadが実行される。
- 反証条件: payloadの実UID/GID、終了code、slurmd log、sacctのいずれかが仮説と一致しなければ仮説を棄却する。

## 隔離設計

候補accountは既存の`testuser`、`testuser2`、`REDACTED_USER`を変更せず、SMD-102専用とする。

| node | user名 | UID:GID | password | HOME |
|---|---|---:|---|---|
| ubuntu2504 controller | `smdmismatch` | `3201:3201` | locked | `/home/smdmismatch` |
| PC-210 worker | `smdmismatch` | `3202:3202` | disabled、login window非表示 | `/Users/smdmismatch` |

安全条件:

1. workerのUID/GID 3201は未使用のままにし、controller credentialの3201が実在する別userへ対応しないようにする。
2. controllerのUID/GID 3202も未使用であることを確認する。
3. payloadは`/tmp`でnumeric UID/GIDとname解決結果を出力するだけにし、HOMEや既存fileへ書き込まない。
4. 1 job、1 task、1分以内とし、失敗時は次項目へ進まない。
5. fail-openを実測した場合はSMD-102を成功扱いにせず、macOS側のlocal identity照合修正を設計する。

## macOS側read-only preflight

2026-09-11に次を確認した。

```text
macos_uid_3201=EMPTY
macos_gid_3201=EMPTY
macos_uid_3202=EMPTY
macos_gid_3202=EMPTY
macos_user_smdmismatch=ABSENT
```

## Ubuntu側で必要なread-only preflight

```bash
getent passwd smdmismatch
getent passwd 3201
getent passwd 3202
getent group 3201
getent group 3202
id smdmismatch
```

全queryが未使用を示しても、次のaccount/group作成はOS state変更なので明示承認までは実行しない。

## Ubuntu側read-only preflight結果

2026-09-11にユーザーが上記6 commandを実行した。5つの`getent`はすべて無出力で、`id`は
`smdmismatch`が存在しないことを返した。

```text
ubuntu_user_smdmismatch=ABSENT
ubuntu_uid_3201=EMPTY
ubuntu_uid_3202=EMPTY
ubuntu_gid_3201=EMPTY
ubuntu_gid_3202=EMPTY
candidate_ids_available=BOTH_NODES
```

これによりID衝突preflightは完了した。次は下記のsystem account作成に対する明示承認が必要である。

- Ubuntu: `smdmismatch` user/groupをUID/GID `3201:3201`、HOME `/home/smdmismatch`、password lockedで作成
- macOS: 同名user/groupをUID/GID `3202:3202`、HOME `/Users/smdmismatch`、password login無効、login window非表示で作成
- 既存`REDACTED_USER`、`testuser`、`testuser2`、`smdtest`のidentityとmembershipは変更しない
- workerのUID/GID 3201とcontrollerのUID/GID 3202は未使用のまま維持する
- account削除はこの承認に含めず、SMD-102完了後に別途判断する

ユーザーは2026-09-11に上記範囲を明示承認した。

## Provisioning script

- Ubuntu: [`smd102_provision_ubuntu.sh`](../../../../contribs/macos-tests/smd102_provision_ubuntu.sh)
- macOS: [`smd102_provision_macos.sh`](../../../../contribs/macos-tests/smd102_provision_macos.sh)

両scriptは承認環境変数、root/OS判定、同名・numeric ID衝突検査、部分作成後の再実行検査を持つ。
加えて、Ubuntuでは3202、macOSでは3201が未使用のままであることを変更前後に検証する。途中失敗時は
accountを自動削除せず、部分状態を表示して停止する。

```text
ubuntu_script_sh_n=PASS
macos_script_sh_n=PASS
approval_gate_without_env_exit=64
macos_nonroot_exit=77
shellcheck=NOT_INSTALLED
ubuntu_script_sha256=93540efa9dcdce7575ea3649316ea12b2d37b72f77f94595a7e792c0660aaea8
macos_script_sha256=6b41f74613ab7faea0b713b0a1f0e10aac9e8384761ebb78c972cf8db378d2b4
```

実行順序はUbuntuを先、macOSを後とする。各scriptの完了markerと反対側IDの`UNUSED`表示を確認するまで
job試験へ進まない。

## Provisioning実行結果と初回macOS failure

Ubuntu側はrootで実行し、予定した隔離accountの作成と検証を完了した。

```text
step=create_group name=smdmismatch gid=3201
step=create_user name=smdmismatch uid=3201 gid=3201
uid=3201(smdmismatch) gid=3201(smdmismatch) groups=3201(smdmismatch)
home=/home/smdmismatch identity_mode=3201:3201:700
password_login=LOCKED
reserved_worker_identity_on_ubuntu=3202:3202:UNUSED
SMD102_UBUNTU_PROVISION_COMPLETE user=smdmismatch uid=3201 gid=3201
```

macOS側の初回実行はgroup/user record作成後にDirectory Serviceのpermission errorで停止した。

```text
step=create_group name=smdmismatch gid=3202
step=create_user name=smdmismatch uid=3202 gid=3202
<main> attribute status: eDSPermissionError
<dscl_cmd> DS Error: -14120 (eDSPermissionError)
SMD102_MACOS_PROVISION_INCOMPLETE rc=40
```

停止後のread-only確認では、同名user/groupとnumeric identityは作成済みだが、HOMEはまだ存在しなかった。

```text
user=smdmismatch uid=3202 gid=3202 home=/Users/smdmismatch shell=/bin/bash
group=smdmismatch gid=3202
id=uid=3202(smdmismatch) gid=3202(smdmismatch) groups=3202,12,61,100,701
home_/Users/smdmismatch=ABSENT
```

初版scriptには属性ごとの`step=`表示がなく、停止後に`GeneratedUID`をroot権限でreadbackしていないため、
失敗した属性をこの出力だけから一意に確定しない。少なくともaccount自体は作成済みで、job投入数は0である。

retry版は次を変更した。

- user属性ごとに`step=set_user_attribute ...`を出し、次回失敗位置を一意にする
- 部分作成済みrecordで`GeneratedUID`が欠けていれば生成・補完する
- Directory Serviceの`Password`属性へ`*`を直接設定せず、macOS標準`pwpolicy -disableuser`を使う
- `pwpolicy -authentication-allowed`の実出力と`IsHidden=1`をreadbackし、認証無効と非表示を機械判定する
- HOME作成、owner/mode、worker上のUID/GID 3201未使用を再検証してから完了markerを出す

```text
macos_script_retry_sh_n=PASS
approval_gate_without_env_exit=64
macos_nonroot_exit=77
macos_script_initial_sha256=6b41f74613ab7faea0b713b0a1f0e10aac9e8384761ebb78c972cf8db378d2b4
macos_script_retry_sha256=4e40ff6e453674481e6521a8fa931fcbffcadabe82d43aec9c1738c55d12dc65
ubuntu_provisioning=PASS
macos_provisioning=READY_TO_RETRY
smd102_job=NOT_RUN
```

`pwpolicy`が対象recordの状態を受理しない場合は完了扱いにせず、その`step=`と実出力を次の修正Evidenceにする。
Mac側の完了marker、認証判定、HOME、反対側ID未使用が揃うまではSMD-102本体jobへ進まない。

## macOS provisioning retry Attempt 2

retry版は部分作成済みaccountを再利用し、認証無効化とHOME作成まで進んだが、`IsHidden`のreadbackが空で
停止した。

```text
step=set_hidden name=smdmismatch
step=disable_authentication name=smdmismatch
Disabling account for user <smdmismatch>
step=create_home path=/Users/smdmismatch
created (/Users/smdmismatch)
error: IsHidden verification failed:
SMD102_MACOS_PROVISION_INCOMPLETE rc=1
```

現在のread-only確認ではaccountへ認証無効化用groupが追加され、HOMEのnumeric owner/modeも予定値になった。

```text
uid=3202(smdmismatch) gid=3202(smdmismatch)
groups=3202,12,61,396(com.apple.access_disabled),100,701
home=/Users/smdmismatch identity_mode=3202:3202:700
macos_uid_gid_3201=UNUSED
```

観測上、`pwpolicy -disableuser`は終了0で`com.apple.access_disabled` membershipを付与した。一方、
`dscl -create /Users/smdmismatch IsHidden 1`もerrorを返さなかったが、直後の同属性readbackは空だった。
したがって認証無効化とHOME作成を失敗原因にせず、今回のblockerをlogin window非表示設定の検証方法へ限定する。
なお、scriptは`IsHidden`失敗で停止したため、`pwpolicy -authentication-allowed`による最終認証判定はまだ
実行されておらず、provisioning全体は未完了のままとする。

Attempt 3版では、実測上保持されなかったuser recordの`IsHidden`を必須条件から外し、目的そのものに対応する
`/Library/Preferences/com.apple.loginwindow`の`HiddenUsersList`へuser名を追加する。既存arrayは上書きせず、
対象名が未登録の場合だけ`defaults ... -array-add`し、直後の`defaults read`から対象名を再取得できた場合だけ
成功とする。認証可否、HOME、3201未使用の既存検証は維持する。認証無効化は先に
`pwpolicy -authentication-allowed`を実行し、すでに禁止済みなら`-disableuser`を再適用しない。

```text
macos_attempt2=BLOCKED_SAFE
smd102_job=NOT_RUN
macos_script_attempt2_sha256=4e40ff6e453674481e6521a8fa931fcbffcadabe82d43aec9c1738c55d12dc65
macos_script_attempt3_sha256=0e558a99a75bf4973a10c9895f87889f0aaa954ea8bda90563000b0e4f6b3962
macos_script_attempt3_sh_n=PASS
approval_gate_without_env_exit=64
macos_nonroot_exit=77
temporary_domain_array_preservation=PASS(existing-user,smdmismatch)
macos_provisioning=READY_TO_RETRY
```

一時domainを用いた機械試験では、既存要素`existing-user`を残して`smdmismatch`を追加し、parserが`YES`を
返し、生成plistの`plutil -lint`も通った。ただしproduction loginwindow domainへの適用はAttempt 3が
完了markerを出すまで実機未確認として扱う。

## macOS provisioning retry Attempt 3 — PASS

Attempt 3版をrootで再実行し、部分作成済みaccountから全検証を完了した。

```text
step=set_loginwindow_hidden name=smdmismatch method=HiddenUsersList
step=disable_authentication name=smdmismatch
authentication_already_disabled=YES
uid=3202(smdmismatch) gid=3202(smdmismatch)
groups=3202,12,61,396(com.apple.access_disabled),100,701
home=/Users/smdmismatch identity_mode=3202:3202:700
authentication_check=User <smdmismatch> is not allowed to authenticate: Credential verification failed because account is disabled.
password_login=DISABLED
controller_identity_on_macos=3201:3201:UNUSED
loginwindow_hidden=YES method=HiddenUsersList
SMD102_MACOS_PROVISION_COMPLETE user=smdmismatch uid=3202 gid=3202
```

これにより、Mac側accountのUID/GID、HOME、password authentication禁止、login window非表示、反対側
numeric identity未使用をすべて実出力から確認した。Ubuntu側の完了結果と合わせ、SMD-102専用accountの
provisioningを`PASS`とする。

生Evidence:

- [`SMD-102-provisioning-macos-attempt3.txt`](./SMD-102-provisioning-macos-attempt3.txt)
  - SHA-256: `b39e05e4ecdfa8b827a7bb8f648dc8fda4d6b275cfefb354db797acf86649982`

```text
ubuntu_provisioning=PASS
macos_provisioning=PASS
provisioning_result=BOTH_NODES_COMPLETE
smd102_job=NOT_RUN
```

## SMD-102本体試験driver

[`smd102_uid_gid_mismatch_driver.sh`](../../../../contribs/macos-tests/smd102_uid_gid_mismatch_driver.sh)を
Ubuntu controller上でrootから実行する。Mac clientから投入するとclient UID 3202が起点になり独立変数が
変わるため、controller上の`smdmismatch=3201:3201`として1件の`srun`だけを投入する。

固定条件と測定項目:

1. controller、node、queue、CPU/memory割当が正常か事前確認する。
2. Ubuntu上の`smdmismatch=3201:3201`と、Ubuntu上の3202未使用を再確認する。
3. `--nodes=1 --ntasks=1 --cpus-per-task=1 --mem=128M --time=00:01:00 --chdir=/tmp`に固定する。
4. payloadはfileを作成せず、固有marker、Job ID、Job user、実UID/GID、name解決、HOME、cwdだけをstdoutへ出す。
5. payloadが実行された場合は意図的にexit 86とし、通信失敗や通常成功と区別する。
6. stdout、stderr、`sacct`、終了後のIDLE・CPUAlloc=0・AllocMem=0・queue空を保存する。
7. 対象job nameを実行ごとに一意化し、失敗時cleanupもそのjobだけに限定する。

判定は次の通りとし、driver単独ではSMD-102を`PASS`へ昇格しない。

- markerあり: `FAIL_OPEN_PAYLOAD_EXECUTED`。実UID/GIDを確認し、Slurm側のlocal identity照合修正へ進む。
- markerなし: `PAYLOAD_NOT_EXECUTED_PENDING_ERROR_REVIEW`。stderr、slurmd log、`sacct`から拒否理由が明確か確認する。
- node/resource未回収: harness完了にせず、対象jobをcleanupして停止する。

local静的確認:

```text
driver_sh_n=PASS
approval_gate_without_env_exit=64
nonroot_exit=77
driver_mode=755
driver_sha256=48a73c9252a8ee7104a2940601a4c55b0b3af12abdec84fc19138c0f457aa67a
smd102_job=READY_TO_RUN
```

実行はdriverをUbuntuの`/tmp`へ転送した後、次の明示gate付きで行う。

```bash
sudo env SMD102_MISMATCH_TEST_CONFIRMED=YES /bin/sh \
  /tmp/smd102_uid_gid_mismatch_driver.sh
```

## 本体試験結果 — FAIL_OPEN / security blocker

2026-09-11にUbuntu controller上からSMD-102専用driverを1回実行した。Job 357は
controller credentialの`uid=3201 gid=3201`を保持したままPC-210上でpayloadを起動した。
PC-210のlocal Directory Serviceでは同名userは`3202:3202`であり、numeric ID 3201に
対応するlocal accountは存在しない。それでもpayloadは実行され、意図した識別用exit code 86で終了した。

```text
test_identity controller=3201:3201 worker_local=3202:3202 user=smdmismatch
srun_rc=86
SMD102_PAYLOAD_EXECUTED=YES
job_id=357
job_user=smdmismatch
actual_uid=3201
actual_gid=3201
actual_user=3201 rc=0
actual_group=3201 rc=0
home=/home/smdmismatch
cwd=/private/tmp
357|smd102-mismatch-20260911T115712|smdmismatch|FAILED|86:0|PC-210
357.0|smd102-mismatch-20260911T115712||FAILED|86:0|PC-210
node_recovered state=IDLE CPUAlloc=0 AllocMem=0 wait_seconds=0
```

観測結果は`H2`を支持し、望ましい`H1`を棄却する。`auth/slurm`によるcredentialの真正性確認と、
worker local accountとのname/UID/GID整合性確認は別問題である。現構成は後者をfail closedにせず、
macOSがpasswd entryなしのnumeric UID/GIDへ権限を落とせるため、誤ったlocal identityでpayloadを
実行する。このため成功条件「誤userで実行せず、原因が明確に記録される」は満たしていない。

生Evidence:

- [`SMD-102-job357-before-fix.txt`](./SMD-102-job357-before-fix.txt)
  - SHA-256: `78ff088c7ea524a2b9e7cd4f7bc79b9a0b7c213383b4c1bbb6ba07222f1fda77`

```text
smd102_result=FAIL_OPEN_PAYLOAD_EXECUTED
payload_executed_as=3201:3201
accounting=FAILED:86:0
resource_recovery=PASS
security_requirement=FAIL
```

ユーザー指示により、SMD-102の修正・build・再試験はいったんスキップする。調査中に作成した
local identity照合のsource案は未build・未検証であり、他の確定済みmacOS修正へ混入させず除去した。
SMD-102専用accountと失敗Evidenceは、後日のfail-closed設計・再試験に備えて保持する。

```text
status=BLOCKED_SECURITY_SKIPPED
source_fix_applied=NO
retest=NOT_RUN
next_workstream=SMD-120
```

## 停止条件

- 両nodeのprovisioning完了と反対側ID未使用のreadback前にjobを投入しない。
- controller UID/GID 3201に対応するaccountをworkerへ作らない。
- 既存userのUID/GIDを変更しない。
- 実機結果前にsource仮説を確定事実として記事へ記載しない。
